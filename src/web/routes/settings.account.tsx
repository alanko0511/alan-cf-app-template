export default function SettingsAccount() {
  return (
    <section>
      <h2>Account</h2>
      <p>
        This one renders at <code>/settings/account</code> — same layout,
        swapped outlet.
      </p>
    </section>
  );
}
