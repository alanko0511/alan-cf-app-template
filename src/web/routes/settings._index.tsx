export default function SettingsIndex() {
  return (
    <section>
      <h2>Profile</h2>
      <p>
        This panel renders in the layout's outlet at <code>/settings</code>.
        Switch to Account — the sidebar stays put, only this content changes.
      </p>
    </section>
  );
}
